/* Aqui creamos la interface de las propiedades de los elementos o datos que iran en nuestra flaglist */
export interface ListItemsOp {
  id: string;
  label: string;
  screenPt: string;
  btnColor: string;
}
